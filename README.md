# Data Cleaning Utility

## What it does
Cleans a messy CSV dataset by handling missing values,
fixing data types, removing duplicates, and standardizing formatting.

## How to run
pip install pandas numpy
python cleaner.py

## Input
data/raw_data.csv — raw messy dataset

## Output
data/cleaned_data.csv — cleaned dataset
cleaning_log.txt — summary of all changes made